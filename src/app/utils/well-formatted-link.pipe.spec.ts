import { WellFormattedLinkPipe } from './well-formatted-link.pipe';

describe('WellFormattedLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new WellFormattedLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
