import { HighlightSearchTermPipe } from './highlight-search-term.pipe';

describe('HighlightSearchTermPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightSearchTermPipe();
    expect(pipe).toBeTruthy();
  });
});
